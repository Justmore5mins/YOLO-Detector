#include <stdio.h>
#include <stdlib.h>
int main(void)
{
	int h,l,high,all,k;
	printf("請輸入梯形上底長:");
	scanf("%d",&h);
	printf("請輸入梯形下底長:");
	scanf("%d",&l);
	printf("請輸入梯形的高:");
	scanf("%d",&high);
    //calculating using formula (h+l)*high/2
	scanf("h+l",&k);//wtf are you wrtiting?
	all=(float)(k/2); //wtf are you writing?
	
	printf("梯形的面積是:%f",(float)(k/2)); //define a variable and show it if you don't sure it can execute successfully or not (limitation of c)
	
	


    system("pause");
    return 0;
}

